# Reusable chains

This directory is reserved for reusable generic `pi-subagents` chains. Do not store a chain tied to one canonical plan here; use [`../saved-plans/`](../saved-plans/README.md) for that purpose.
