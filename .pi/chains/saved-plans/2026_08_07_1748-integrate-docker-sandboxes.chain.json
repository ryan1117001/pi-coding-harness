{
  "name": "saved-plan-2026_08_07_1748-integrate-docker-sandboxes",
  "description": "Plan-specific execution chain for docs/prompts/2026_08_07_1748-integrate-docker-sandboxes/README.md. It cannot run before explicit user approval and plan-index registration; the approved repository-managed Pi pin is @earendil-works/pi-coding-agent@0.84.1.",
  "chain": [
    {
      "agent": "scout",
      "phase": "Context",
      "label": "Confirm approved Docker Sandboxes scope and readiness",
      "task": "Read the explicitly approved and plan-index-registered canonical plan at docs/prompts/2026_08_07_1748-integrate-docker-sandboxes/README.md, prior Sandbox research, current host/Dev Container/Pi configuration, Compose topology, and installed sbx 0.38.0 help. Do not modify files or launch a sandbox. Confirm the plan consistently fixes the repository-managed authority at @earendil-works/pi-coding-agent@0.84.1 with no remaining Pi-version approval gate, and that Dev Container setup/smoke and Sandbox bootstrap will consume or assert it. Verify the explicit trusted read-only user Pi opt-in contract (only validated regular non-symlink ~/.pi/agent/settings.json and directory non-symlink ~/.pi/agent/extensions as separate read-only workspaces, sandbox-local wiring, structural top-level legacy apiKeys refusal without value output, no whole agent directory, no packages-only projection or individually copied extensions), exact 0.38.0 value-free global and named secret inventory interfaces, including globally injected service/registry credential contamination; effective policy inventory/check/removal interfaces; template, clone-source, attach, port, copy/recovery, and cleanup evidence gaps; and the stated toolchain/version authorities. Record name-only sandbox, secret, policy, listener, template/export, worktree, and remote baselines without inspecting values. Begin the report with exactly READY only if every approval gate is met and no unapproved product, security, architecture, credential, platform, or version choice remains. Otherwise begin with exactly BLOCKED and list the gates; every downstream writer must then be a no-op.",
      "output": "context.md",
      "outputMode": "file-only"
    },
    {
      "agent": "worker",
      "phase": "Implementation",
      "label": "Implement the optional Sandbox runtime",
      "task": "Read the approved canonical plan and context below. If the previous report is BLOCKED, incomplete, does not begin READY, or reports any unresolved decision, do not modify files; return BLOCKED. You are the sole implementation/configuration writer. Implement only the approved plan. Add tools/pi-version.txt containing exactly @earendil-works/pi-coding-agent@0.84.1 and make repository-controlled Dev Container setup/smoke, Sandbox bootstrap, contracts, and version documentation consume or assert it. Fail closed unless the installed version/help and authorized disposable live evidence are exactly sbx 0.38.0; prove lifecycle syntax and behavior before committing lifecycle commands. First capture intended RED failures with fake sbx, then GREEN. Implement the unique dedicated tracked-only Git worktree clone default and live-proof gate; reject .env*, .npmrc, .pi/npm, .pi/git, auth.json, node_modules, .venv, credential/private-key files, ignored/untracked contamination, symlinks, and path escapes before sbx, with disposable sentinel coverage and clone-source absence proof. Direct mode is an explicit trusted full-checkout mutation opt-in and omits clone rather than inventing --direct. Add a distinct attach path that rejects creation-only mode/template/workspace/publication options and never silently reuses an unverifiable sandbox. Implement an explicit trusted full read-only user Pi settings/extensions opt-in analogous to the Dev Container pattern. Accept only a validated regular non-symlink ~/.pi/agent/settings.json and a validated directory non-symlink ~/.pi/agent/extensions, expose them as separate extra read-only Sandbox workspaces, and wire them into sandbox-local Pi paths without mounting the whole agent directory. Structurally reject a top-level legacy apiKeys field without printing values. Treat all other settings and extension content as user-inspected trusted readable/executable input that may contain commands, secrets, and host-only paths; do not sanitize/project only packages or individually copy selected extensions. Install global package declarations as Linux-native copies into sandbox-local writable package/Git state. Prove sandbox settings writes/configuration changes fail while enabled or require disabling the opt-in, and document concurrent host rewrites as unsupported. Never expose host auth.json, sessions, trust, models, prompts, skills, themes, npm, Git state, caches, SSH, general home, or Docker socket. Implement value-free global/named secret inventory and provider-smoke refusal when existing global service/registry credentials could authenticate; accept test secrets only through interactive or approved-secret-manager stdin; prohibit token flags, literals, set -x, dumps, and secret import; prove intended scoped names; remove each scoped secret before VM removal and prove name-only absence while stating upstream revocation is separate. Inspect effective policy including inactive/global/profile/kit/organization rules, reject broad active allows, positively and negatively check approved/unapproved destinations and ports, record each added local rule ID, and remove/prove absence without global or wildcard rules. Sequence unique-name collision refusal, staging worktree, named shell sandbox and exact built-in template, temporary bootstrap egress, root-only OS provisioning, normal-user Node 24.19.0/pnpm 11.20.0/Python 3.14.4/uv 0.12.2/@earendil-works/pi-coding-agent@0.84.1/locked dependencies/Chromium, verification, and smoke-proven Pi startup. Run frozen pnpm and locked uv sync equivalents twice and prove idempotence, ownership, and unchanged tracked state. Prohibit template save/load and custom tags/exports. For shell-docker, prove a sandbox-local daemon/socket with no host context/container/image visibility and run blocked probes outside and inside a nested container. Publish no outer port by default or 5432 by default; discover optional ephemeral loopback-only 4200/8000 mappings. Test exact host.docker.internal/localhost:<PORT> pairing only when available. Add trap-driven owned-resource teardown and host-side recovery through the generated sandbox remote or live-proven copy interface; never configure upstream sandbox credentials or use reset, bulk removal, global secrets, or global/wildcard policy. Add package.json sandbox:test for fake-sbx only and no lifecycle hook. Do not edit living documentation, the canonical plan, or docs/prompts/README.md. Run live smoke only on an authorized eligible host; report gaps. Return changed files, RED/GREEN evidence, commands, live matrix, cleanup inventories, and residual risks.\n\nContext:\n{previous}",
      "output": "implementation.md",
      "outputMode": "file-only",
      "progress": true
    },
    {
      "agent": "technical-writer",
      "phase": "Documentation",
      "label": "Document the optional Sandbox runtime and security boundary",
      "task": "Read the approved plan and implementation report below. If the previous report is BLOCKED, incomplete, or lacks implementation artifacts/evidence, do not modify files; return BLOCKED. Inspect the actual implementation and evidence before writing. Perform only the substantial living-documentation work mapped by the plan: add/index the design record and operational reference and update architecture, onboarding, agent policy, Dev Container comparison, Pi/version/configuration ownership, and any introduced environment variables. Preserve host and trusted Dev Container paths. Document the exact repository-managed @earendil-works/pi-coding-agent@0.84.1 pin consumed/asserted by Dev Container setup/smoke and Sandbox bootstrap; shell agent versus exact built-in shell/shell-docker selection; generated tracked-only staging worktree versus trusted direct full-checkout mutation; explicit attach constraints; the trusted optional separate read-only settings-file/extensions-directory workspaces, their regular non-symlink validation, sandbox-local wiring, whole-agent exclusion, structural top-level legacy apiKeys refusal without values, trusted executable/readable input warning, Linux-native sandbox-local package/Git installs, settings-write behavior, and unsupported concurrent host rewrites; host-stored proxy credentials versus sandbox-local auth.json; value-free global/scoped secret preflight, stdin-only approved input, scoped removal, and separate upstream revocation; effective-policy positive/negative checks and rule-ID cleanup; sandbox-local nested Docker and nested egress probes; no default outer ports or 5432 and optional discovered ephemeral loopback 4200/8000; exact evidence-bounded llama.cpp pairing; host-side recovery and deterministic teardown. State that Codex Enterprise, GitHub Copilot Enterprise, and Anthropic Enterprise are unsupported/unverified until the full approved matrix is recorded. State that Cursor remains optional and requires separate extension and credential evidence despite the approved Pi pin. Prohibit template save/load and distinguish generic CI fake-sbx coverage from manually authorized live smoke. Bound all CLI/platform/provider/template claims to recorded evidence, require exact sbx 0.38.0 help/live evidence before documenting committed lifecycle commands, and fail closed on any other version or behavioral mismatch. Do not edit implementation/configuration, the canonical plan, or docs/prompts/README.md. Validate links and Markdown and report residual gaps.\n\nImplementation report:\n{previous}",
      "output": "documentation.md",
      "outputMode": "file-only",
      "progress": true
    },
    {
      "parallel": [
        {
          "agent": "reviewer",
          "phase": "Review",
          "label": "Review Sandbox security and credential isolation",
          "task": "Review the current diff against the explicitly approved canonical plan. Do not modify files or use credentials. Verify lifecycle commands were committed only after exact sbx 0.38.0 help and disposable live evidence, with fail-closed behavior for any mismatch. Verify the exact @earendil-works/pi-coding-agent@0.84.1 authority is consumed/asserted by Dev Container setup/smoke and Sandbox bootstrap while Cursor remains optional and separately evidenced. Verify dedicated tracked-only staging clone selection, prohibited ignored-file/symlink sentinel refusal and /run/sandbox/source absence, direct full-checkout warnings, attach verification, and no host auth/Pi/npm/Git/SSH/Docker-socket exposure. Verify the explicit trusted opt-in exposes only a validated regular non-symlink ~/.pi/agent/settings.json and directory non-symlink ~/.pi/agent/extensions as separate read-only workspaces wired to sandbox-local Pi paths; rejects top-level legacy apiKeys structurally without printing values; excludes the whole agent directory and all other host Pi/auth/session/trust/model/prompt/skill/theme/npm/Git/cache/SSH/home state; treats remaining settings/extensions as user-inspected trusted readable/executable input; installs Linux-native global packages into sandbox-local writable package/Git state; makes settings writes fail or require disabling the opt-in; documents concurrent host rewrites as unsupported; and does not packages-only project, sanitize, or individually copy extensions. Verify value-free global/named secret inventory refuses contaminating global service/registry credentials, stdin-only approved secret input, intended scoped-name proof, scoped removal/absence, and separate upstream revocation. Verify effective policy includes inactive and broader applicable rules, rejects broad active allows, performs positive and negative host/port/local-range/wildcard checks, records/removes local rule IDs, and leaves no orphan. Verify nested blocked probes, sandbox-local Docker daemon/socket, no host context/container/image visibility, loopback-only outer publication with no default 5432, evidence-bounded llama.cpp pairing, custom-template prohibition, trap-driven owned cleanup, host-side recovery, and unsupported enterprise language. Return only evidence-backed findings with severity and file/line references, distinguishing blockers from accepted documented risks.\n\nLatest report:\n{previous}",
          "output": "security-review.md",
          "outputMode": "file-only"
        },
        {
          "agent": "reviewer",
          "phase": "Review",
          "label": "Review reproducibility and runtime contracts",
          "task": "Review the current diff against the explicitly approved canonical plan. Do not modify files. Verify READY/BLOCKED gates were honored; RED/GREEN fake-sbx isolation; exact sbx 0.38.0 help/live-evidence fail-closed behavior before lifecycle commands are committed; live-proven built-in template, clone-source, attach, Pi-start, port, secret, policy, recovery, and cleanup syntax; and omission of invented direct syntax. Verify tools/pi-version.txt contains exactly @earendil-works/pi-coding-agent@0.84.1 as the single repository-managed Pi authority consumed/asserted across Dev Container setup/smoke and Sandbox bootstrap paths, with Cursor optional and separately evidenced, without claiming arbitrary host control. Verify Node 24.19.0, pnpm 11.20.0, Python 3.14.4, uv 0.12.2, frozen pnpm, locked uv, normal-user state, root-only OS packages, default Chromium, second-run idempotence, ownership, and unchanged tracked/generated state. Verify the explicit trusted user Pi opt-in uses only separately validated regular non-symlink settings and directory non-symlink extensions read-only workspaces with sandbox-local wiring; structural top-level legacy apiKeys rejection without values; user-inspected trusted-content warnings; Linux-native global packages in sandbox-local writable package/Git state; settings-write failure or opt-in disablement; unsupported concurrent host rewrites; and no whole-agent mount, packages-only projection, sanitization, or individual extension copying. Verify pnpm sandbox:test is fake-only and no lifecycle/default command launches sbx. Verify unique-name collision refusal, explicit attach contract, host-side recovery, signal-safe owned-resource teardown/baseline comparison, no reset/bulk/global actions, no template save/load/export, shell-docker VM-local daemon and nested egress, no default outer port/5432, ephemeral loopback discovery, Nx sync/discovery plus applicable lint/typecheck/test/build, API tests, and Chromium launch. Return evidence-backed findings with severity and file/line references and explicit live-evidence gaps.\n\nLatest report:\n{previous}",
          "output": "reproducibility-review.md",
          "outputMode": "file-only"
        }
      ],
      "concurrency": 2
    },
    {
      "agent": "worker",
      "phase": "Fixes",
      "label": "Apply in-scope Sandbox review fixes",
      "task": "Read the approved plan, current diff, and both reports below. If the preceding implementation/documentation reports were BLOCKED or incomplete, or a reviewer identifies an unresolved product/security/architecture/version decision, do not modify files; return BLOCKED for parent/user synthesis. You are the sole fix writer. Apply only concrete in-scope fixes authorized by the plan. Preserve the fail-closed requirement that exact sbx 0.38.0 help and authorized disposable live evidence precede committed lifecycle commands. Preserve the exact @earendil-works/pi-coding-agent@0.84.1 authority, tracked-only staging clone, trusted direct opt-in, explicit attach, separate validated read-only user settings/extensions workspaces with sandbox-local wiring, top-level legacy apiKeys refusal, trusted-input warnings, Linux-native sandbox-local package/Git state, read-only write behavior and unsupported concurrent host rewrites, whole-agent and other host-state exclusions, no packages-only projection or per-file extension copying, separated proxy/OAuth ownership, global-credential contamination refusal, stdin-only scoped secrets and name-only cleanup, effective-policy positive/negative checks and rule-ID cleanup, sandbox-local nested Docker, no default outer ports/5432, exact evidence-bounded llama.cpp pairing, custom-template prohibition, deterministic owned teardown, host-side recovery, bounded provider claims, and generic-CI fake-only suite. Do not introduce a provider, credential path, platform, architecture, template, or version decision. Do not edit the canonical plan or docs/prompts/README.md. Rerun focused contracts and authorized applicable live smoke; report changes, outcomes, cleanup inventories, remaining findings, and gaps.\n\nReviewer findings:\n{previous}",
      "output": "fixes.md",
      "outputMode": "file-only",
      "progress": true
    },
    {
      "agent": "reviewer",
      "phase": "Validation",
      "label": "Validate final Sandbox integration",
      "task": "Validate the final diff against the explicitly approved canonical plan and fix report. Do not modify files. If the fix report is BLOCKED or an approval gate remains unresolved, return BLOCKED. Require exact sbx 0.38.0 installed help and authorized disposable live evidence for every committed lifecycle command, failing closed on any mismatch. Run shell syntax, pnpm sandbox:test fake-sbx contracts, root Compose rendering, Nx sync/discovery and applicable lint/typecheck/test/build, locked Python sync/API tests, Chromium launch, Markdown/link checks, and git diff/status checks. Confirm tools/pi-version.txt is exactly @earendil-works/pi-coding-agent@0.84.1 and is consistent across repository-controlled Dev Container setup/smoke and Sandbox bootstrap paths, while Cursor remains optional and separately evidenced and host parity is not overclaimed. Confirm clone sentinel/path/symlink refusals and live clone-source absence; full direct warning; attach restrictions; the explicit trusted opt-in's regular non-symlink settings-file and directory non-symlink extensions-root validation, separate read-only workspace exposure, sandbox-local wiring, top-level legacy apiKeys rejection without values, trusted-content warning, Linux-native sandbox-local package/Git installs, settings-write failure/disable path, unsupported concurrent host rewrites, and prohibition on whole-agent mounts, packages-only projection, sanitization, and individual extension copying; no excluded host auth/session/trust/model/prompt/skill/theme/npm/Git/cache/SSH/home/socket exposure; value-free global/named service and registry credential inventory/refusal; intended scoped secret names, removal, and absence; no values in repository/helper arguments/output/VM; effective policy including broad applicable rules; positive and negative checks; local rule-ID cleanup; nested outer/container blocked probes; sandbox-local daemon with no host context/container/image access; no default outer port or 5432 and ephemeral loopback-only requested mappings; exact optional llama.cpp pairing; and unsupported enterprise matrix. On an eligible authorized host, require a unique collision-free name and EXIT/INT/TERM owned-resource cleanup; never attach/remove pre-existing resources or use reset, bulk removal, global secrets, global/wildcard policy, or template save/load. Validate host-side recovery, repeated bootstrap idempotence/ownership/unchanged tracked state, Pi startup, shell-docker/Compose, port inventory, and post-teardown sandbox, secret-name, policy, listener, custom-template/export, worktree, and remote inventories against baseline. Inspect template JSON and filesystem for unexpected tags/tar exports. If live cases are unavailable, mark them not run and reject corresponding support claims. Return commands and pass/fail outcomes, blockers, exact evidence matrix, cleanup state, and residual risks.\n\nFix report:\n{previous}",
      "output": "final-validation.md",
      "outputMode": "file-only"
    }
  ]
}
